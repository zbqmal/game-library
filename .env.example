# Firebase Admin SDK Configuration
# These environment variables are required for visit tracking functionality
# Get these values from your Firebase project's service account key

# Your Firebase project ID
FIREBASE_PROJECT_ID=your-project-id

# The service account email (format: firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com)
FIREBASE_CLIENT_EMAIL=your-client-email@your-project-id.iam.gserviceaccount.com

# The service account private key (include quotes, will be automatically processed)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Note: The FIREBASE_PRIVATE_KEY should contain the literal \n characters
# They will be replaced with actual newlines automatically by the app

# Firebase Client SDK Configuration (Public - for client-side real-time listeners)
# These are public environment variables that can be safely exposed to the browser
# Get these values from your Firebase project settings

# Your Firebase API key
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key

# Your Firebase project ID (can be same as FIREBASE_PROJECT_ID above)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Firebase auth domain (format: your-project-id.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase app ID
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
