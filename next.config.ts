import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Uncomment below for mobile deployment with Capacitor
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
